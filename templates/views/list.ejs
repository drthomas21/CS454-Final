<div class="row" ng-controller="WishListCtrl" ng-init="getList()">
	<div class="col-sm-8 col-sm-offset-2">
		<h3>All Wishlists</h3>
		<table class="table table-border">
			<tr>
				<th>#</th>
				<th>Id</th>
				<th>Name</th>
				<th>Description</th>
				<th>Items</th>
				<th>Actions</th>
			</tr>
			<tr ng-repeat="Model in allWishlists">
				<td>{{$index+1}}</td>
				<td><a ng-href="/{{Model._id}}">{{Model._id}}</a></td>
				<td><a ng-href="/{{Model._id}}">{{Model.name}}</a></td>
				<td>{{Model.description.substr(0,75)}}</td>
				<td>{{Model.items.length}}</td>
				<td>
					<button class="btn btn-sm btn-warning btn-block" ng-click="editModel(Model)">Edit</button>
					<button class="btn btn-sm btn-danger btn-block" ng-click="removeModel(Model)">Delete</button>
				</td>
			</tr>
		</table>
	</div>
</div>